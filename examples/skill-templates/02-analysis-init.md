# 示例 2：创建分析 Skill（data-analyzer）

```bash
owlclaw skill init --template analysis/data-analyzer \
  --param "skill_name=data-analyzer,skill_description=Analyze daily metrics,data_sources=sales,inventory" \
  --output capabilities
```

