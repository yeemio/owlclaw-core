# Fish completion for owlclaw CLI.

complete -c owlclaw -f -a "init" -d "Initialize config"
complete -c owlclaw -f -a "reload" -d "Reload config"
complete -c owlclaw -f -a "db" -d "Database commands"
complete -c owlclaw -f -a "skill" -d "Skill commands"
complete -c owlclaw -f -a "memory" -d "Memory commands"
complete -c owlclaw -f -a "agent" -d "Agent commands"
complete -c owlclaw -f -a "trigger" -d "Trigger template commands"
